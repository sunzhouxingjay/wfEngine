A-B-C-D
A执行服务发送数据到服务器，服务器执行完调用B上的服务，取得处理完成的数据。A将自身的taskid传到服务器上，服务器将数据处理完成后，通过a.taskid获得b.taskid从而定位到B,然后服务器自动调用B上的服务，获得处理的数据。B这里接收到数据后直接完成？？


4个属性分别是工作流实例Id,taskid,taskName,task任务状态（设置变量status   0：服务还未开始执行 1：服务已执行，等待数据返回 2：任务可完成）
serviceType分别为auto，longtime，receive(auto执行成功后status由0变2，longtime会将variables发送出去，成功后status置1，接收数据成功后置2，)。
参数还是不由activiti任务本身提供，服务的参数还是由服务发起者提供，activiti执行服务后，将返回结果返回给用户服务发起者本身。
逻辑？？
目标
1/实现task任务可以请求服务
2/部署，实例化与完成，谁部署？？谁负责实例化？？谁完成？？？
3/调用服务与complete是否放在一起？若放在一起，同步服务简单明了，长时间的服务，发送数据发送后收到ok为发送成功，接收
4/
